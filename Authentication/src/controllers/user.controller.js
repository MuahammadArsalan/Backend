import Users from '../models/user.model.js'
import  jwt  from 'jsonwebtoken'


const generateAccessToken  = (user) =>{
// return jwt.sign({email:user.email},) //iske aage ka JWT kiweb se url milega wo lagega
}